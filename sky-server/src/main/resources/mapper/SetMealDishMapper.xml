<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetMealDishMapper">

    <select id="countByDishIdForBatch" resultType="java.lang.Integer">
        select count(setmeal_id) from sky_take_out.setmeal_dish where dish_id in
            <foreach collection="dishIds" item="dishId" open="(" separator="," close=")">#{dishId}</foreach>
    </select>

    <insert id="insertForBatch">
        insert into sky_take_out.setmeal_dish
            (setmeal_id, dish_id, name, price, copies)
        values
            <foreach collection="setMealDishes" item="smd" separator=",">
                (#{smd.setmealId},#{smd.dishId},#{smd.name},#{smd.price},#{smd.copies})
            </foreach>
    </insert>

    <select id="getDishes" resultType="com.sky.entity.Dish">
        select d.* from sky_take_out.setmeal_dish sd right outer join sky_take_out.dish d on sd.dish_id = d.id where sd.setmeal_id=#{id}
    </select>

    <delete id="deleteBatch">
        delete from sky_take_out.setmeal_dish where setmeal_id in
            <foreach collection="setMealIds" item="smId" open="(" separator="," close=")">#{smId}</foreach>
    </delete>

    <select id="getBySid" resultType="com.sky.entity.SetmealDish">
        select * from sky_take_out.setmeal_dish where setmeal_id=#{id}
    </select>

    <delete id="deleteBySid">
        delete from sky_take_out.setmeal_dish where setmeal_id=#{id}
    </delete>

    <select id="getsmlByDishId" resultType="com.sky.entity.Setmeal">
        select s.* from sky_take_out.setmeal s left join sky_take_out.setmeal_dish sd on s.id = sd.setmeal_id where sd.dish_id=#{id}
    </select>
</mapper>
