<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.DishFlavorMapper">

    <insert id="insertForBatch">
        insert into sky_take_out.dish_flavor (dish_id, name, value) values
        <foreach collection="flavors" item="f" separator=",">
            (#{f.dishId},#{f.name},#{f.value})
        </foreach>
    </insert>
    <delete id="deleteByIdForBatch">
        delete from sky_take_out.dish_flavor where dish_id in
            <foreach collection="dishIds" item="dishId" open="(" separator="," close=")">#{dishId}</foreach>
    </delete>
    <select id="getByDishId" resultType="com.sky.entity.DishFlavor">
        select * from sky_take_out.dish_flavor where dish_id=#{dishId};
    </select>
    <delete id="deleteByDishId">
        delete from sky_take_out.dish_flavor where dish_id=#{dishId};
    </delete>

</mapper>